"use client"
type SwapperGridProps = {
    items?: any[];
    display: number;
    firstItem?: number;
    className?: string;
}
// carousel on roids
export const SwapperGrid = ( { items, display=1, firstItem=1, className="" }:SwapperGridProps ) => <></>

